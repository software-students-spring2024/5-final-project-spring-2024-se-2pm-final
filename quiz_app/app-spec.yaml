name: quiz-app
region: nyc
services:
- name: web-service
  image:
    registry: dockerhub
    username: ${{ secrets.DOCKER_USERNAME }}
    tag: latest
  environment_slug: python
  http_port: 3000
  routes:
    - path: /
  instance_size_slug: basic-xxs
  instance_count: 1

  

